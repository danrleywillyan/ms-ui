import { TransactionsFilterPipe } from './transactions-filter.pipe';

describe('TransactionsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TransactionsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
